# MarsAI 命令手册

本文档详细说明 MarsAI QQ 机器人的所有可用命令。这些命令可用于管理 AI 对话、控制模型行为和处理会话历史。

## 快速命令参考

| 命令 | 描述 | 参数 | 权限 |
|------|------|------|------|
| `/help` | 显示所有可用命令 | 无 | 所有用户 |
| `/clear` | 清除当前会话历史 | 无 | 所有用户 |
| `/reset` | 重置对话（同 `/clear`） | 无 | 所有用户 |
| `/prompt` | 显示、更新系统prompt或切换预设 | `[text|数字|list]` | 所有用户 |
| `/model` | 显示或切换 AI 模型 | `[name]` | 所有用户 |
| `/summary` | 总结当前对话内容 | 无 | 所有用户 |
| `/history` | 查看最近对话历史 | `[n]` | 所有用户 |
| `/status` | 显示当前系统状态 | 无 | 所有用户 |

## 命令详解

### `/help`
显示所有可用命令及其简要说明。

**语法：**
```
/help
```

**示例：**
```
用户：/help
AI：可用命令：
/clear - 清除当前会话历史
/prompt [text] - 显示或更新系统prompt（更新时会清除历史）
/model [name] - 切换或显示当前模型
/summary - 总结当前对话内容
/history [n] - 显示最近n条历史消息（默认10条）
/status - 显示当前状态（模型、prompt长度等）
/reset - 重置对话（同/clear）
/help - 显示此帮助信息
```

---

### `/clear`
清除当前会话的所有历史记录。这个命令不会影响系统 prompt 或模型设置，只会清空对话历史。

**语法：**
```
/clear
```

**示例：**
```
用户：/clear
AI：已清除当前会话历史
```

**注意：**
- 清除后无法恢复历史记录
- 每个会话（私聊或群聊）的历史是独立的
- 清除后，AI 将开始一个全新的对话

---

### `/reset`
功能与 `/clear` 相同，提供另一种方式重置对话。

**语法：**
```
/reset
```

**示例：**
```
用户：/reset
AI：对话已重置
```

---

### `/prompt`
显示当前系统 prompt、更新为新的 prompt 或切换预设 prompt。**重要：更新 prompt 时会自动清除当前会话历史**，以避免旧对话干扰新的角色设定。

**语法：**
```
/prompt                    # 显示当前 prompt 预览
/prompt <new_prompt_text>  # 更新 prompt 并清除历史
/prompt <数字>             # 切换到对应数字的预设 prompt
/prompt list               # 列出所有可用预设
```

**示例：**
```
# 查看当前 prompt
用户：/prompt
AI：当前prompt预览（319字符）：
你是一位深度精通《Minecraft》（我的世界）全版本、全机制的资深玩家...
使用 /prompt <新prompt> 来更新，或使用 /prompt <数字> 切换预设，/prompt list 列出预设

# 更新 prompt
用户：/prompt 你是一个专业的 Python 编程助手，精通各种 Python 库和框架。
AI：系统prompt已更新，当前会话历史已清除

# 切换预设 prompt
用户：/prompt 1
AI：已切换到预设 1，当前会话历史已清除

# 列出预设
用户：/prompt list
AI：可用预设：
1: 你是一位深度精通《Minecraft》（我的世界）全版本...
2: 你是一个乐于助人的AI助手，请用友好、简洁的方式...
3: 你是一个编程专家，擅长Python、JavaScript、Java...
```

**注意：**
- 更新 prompt 后，之前的对话历史会被自动清除
- Prompt 最大长度取决于使用的 AI 模型
- 修改 prompt 会改变 AI 的行为和角色设定
- Prompt 保存在 `session/bot_config.json` 中，重启后仍有效
- 预设 prompt 也保存在配置文件中，可手动编辑添加更多预设

---

### `/model`
显示当前使用的 AI 模型或切换到其他可用模型。

**可用模型：**
- **云端模型**：
  - `DeepSeek Chat` (deepseek-chat) - 默认模型
- **本地模型**（需安装 Ollama）：
  - `gemma3:12b` (gemma3:12b)
  - `gpt-oss:20b` (gpt-oss:20b)
  - `geem3` (geem3)

**语法：**
```
/model                    # 显示当前模型
/model <model_name>      # 切换到指定模型
```

**示例：**
```
# 查看当前模型
用户：/model
AI：当前模型：DeepSeek Chat

# 切换模型
用户：/model gemma3:12b
AI：已切换模型到：gemma3:12b
```

**注意：**
- 切换模型不会清除对话历史
- 本地模型需要安装并运行 Ollama
- 云端模型需要配置 `DEEPSEEK_API_KEY` 环境变量
- 模型切换后，新的回复将使用新模型生成

---

### `/summary`
使用 AI 分析并总结当前会话的对话内容。提取关键信息、讨论主题和结论。

**语法：**
```
/summary
```

**示例：**
```
用户：/summary
AI：对话总结：
本次对话主要讨论了 Minecraft 的红石电路设计。用户询问了自动农场的工作原理，
AI 详细解释了活塞、红石火把和比较器的配合使用。对话还涉及了不同版本（Java版
和基岩版）的差异。最后用户得到了一个可用的自动甘蔗农场设计方案。
```

**注意：**
- 总结内容由 AI 生成，可能不完美
- 需要至少有一些对话历史才能总结
- 总结会消耗一次 API 调用
- 总结提示是独立的，不会影响系统 prompt

---

### `/history`
查看最近的对话历史记录。默认显示最近 10 条消息，可指定数量。

**语法：**
```
/history          # 显示最近10条消息
/history <n>      # 显示最近n条消息（1-50）
```

**示例：**
```
# 查看最近5条消息
用户：/history 5
AI：最近 5 条消息：
1. [用户] 如何建造自动农场？
2. [AI] 自动农场需要红石电路控制...
3. [用户] 需要哪些材料？
4. [AI] 主要需要：活塞、红石火把...
5. [用户] 谢谢，我明白了

# 默认查看最近10条
用户：/history
AI：最近 10 条消息：
...
```

**注意：**
- 最多显示 50 条历史记录
- 消息内容会被截断（前100字符）
- 显示格式：`[用户]` 表示用户消息，`[AI]` 表示 AI 回复
- 历史记录存储在 `session/user_sessions.json` 中

---

### `/status`
显示当前系统的状态信息，包括模型设置、prompt 长度和会话历史大小。

**语法：**
```
/status
```

**示例：**
```
用户：/status
AI：当前状态：
模型：DeepSeek Chat
Prompt长度：319 字符
会话历史：15 条消息
```

**显示的信息包括：**
- 当前使用的 AI 模型
- 系统 prompt 的字符数
- 当前会话的历史消息数量

## 使用场景示例

### 场景1：切换 AI 角色
```
用户：/prompt 你是一个专业的英语学习助手，帮助我练习英语对话。
AI：系统prompt已更新，当前会话历史已清除

用户：Hello, can you help me practice English?
AI：当然可以！我是你的英语学习助手。我们可以从日常对话开始...
```

### 场景2：清理旧对话后开始新话题
```
用户：/clear
AI：已清除当前会话历史

用户：我们来聊聊编程吧
AI：（开始全新的对话，没有之前的上下文）
```

### 场景3：了解对话概况
```
（经过一段较长的对话后）
用户：/summary
AI：对话总结：
本次对话主要讨论了 Python 异步编程...
```

### 场景4：检查系统状态
```
用户：/status
AI：当前状态：
模型：DeepSeek Chat
Prompt长度：256 字符
会话历史：42 条消息
```

## 常见问题

### Q1：为什么更新 prompt 后历史被清除了？
A：这是设计行为。为了避免旧的对话历史干扰新的角色设定，更新 prompt 时会自动清除历史。如果需要保留历史，请先使用 `/history` 导出重要信息。

### Q2：命令在群聊中如何使用？
A：在群聊中，需要 @机器人 后发送命令，例如：`@MarsAI /help`

### Q3：对话历史保存在哪里？
A：所有会话历史都保存在 `session/user_sessions.json` 文件中。私聊和群聊的历史分开存储。

### Q4：最多可以保存多少条历史记录？
A：每个会话最多保存 200 条最新的消息，旧消息会被自动删除。

### Q5：如何完全重置所有设置？
A：删除 `session/` 目录下的所有文件，然后重启机器人。

### Q6：为什么 `/summary` 有时会失败？
A：可能的原因：
1. 没有对话历史
2. API 调用失败
3. 网络连接问题
4. 模型暂时不可用

## 文件结构
- `session/user_sessions.json` - 存储所有会话历史
- `session/bot_config.json` - 存储系统配置（模型和 prompt）
- `src/plugins/mars_ai/__init__.py` - 命令实现代码

## 更新日志
- v1.0 - 初始版本，包含基本命令
- v1.1 - 添加 `/summary`, `/history`, `/status`, `/reset` 命令
- v1.2 - 改进 `/prompt` 命令，添加预览功能

---

**提示**：重启机器人后所有命令立即生效。如果遇到问题，请检查机器人是否正常运行。